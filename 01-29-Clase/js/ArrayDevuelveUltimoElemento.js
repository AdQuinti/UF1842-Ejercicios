/** Ejercicio REPASO
 * Array que devuelva último elemento
 */

const colores = ['azul','rojo','amarillo'];
// funcion fleca q devuelve ultima posición
const ultimoElmento = (arr) => arr[arr.length-1];

document.write(ultimoElmento(colores));